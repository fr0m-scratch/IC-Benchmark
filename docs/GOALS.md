
```markdown
# Project Goals (for the code agent)

- Prove: **SLM > sensitivity to interface complexity** than LLM.
- Show: **any single complexity-aware stage** (retrieval rerank OR interface wrapper OR slot-by-slot filling) substantially **narrows SLM/LLM gap**.
- Provide: reproducible open artifacts (IC scorer, wrappers, task generator, logs, plots).
